apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: portail
  namespace: portail-system
  labels:
    app: portail
spec:
  selector:
    matchLabels:
      app: portail
  template:
    metadata:
      labels:
        app: portail
    spec:
      serviceAccountName: portail-controller
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - name: portail
          image: portail:latest
          args: ["--kubernetes"]
          securityContext:
            capabilities:
              add:
                - BPF
                - NET_ADMIN
                - NET_BIND_SERVICE
                - SYS_ADMIN
          volumeMounts:
            - name: bpf
              mountPath: /sys/fs/bpf
            - name: tls-certs
              mountPath: /etc/portail/certs
              readOnly: true
      volumes:
        - name: bpf
          hostPath:
            path: /sys/fs/bpf
            type: DirectoryOrCreate
        - name: tls-certs
          secret:
            secretName: portail-tls
            optional: true
