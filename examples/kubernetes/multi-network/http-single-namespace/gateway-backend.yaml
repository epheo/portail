apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gw-backend
spec:
  gatewayClassName: portail
  addresses:
  - type: portail.epheo.eu/Network
    value: udn-backend
  listeners:
  - name: http
    port: 8080
    protocol: HTTP
