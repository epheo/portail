apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gw-backend
spec:
  gatewayClassName: portail
  addresses:
  - type: IPAddress
    value: "10.10.2.1"  # Portail's IP on udn-backend
  listeners:
  - name: http
    port: 80
    protocol: HTTP
